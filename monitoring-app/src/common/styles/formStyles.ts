import { StyleSheet } from 'react-native';

export const formStyles = StyleSheet.create({
  welcomeContainer: {
    width: '100%',
    paddingVertical: 25,
    marginBottom: 5,
  },

  welcomeHeader: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
  },

  welcomeText: {
    opacity: 0.9,
    textAlign: 'center',
  },

  formContainer: {
    width: '100%',
    maxWidth: 350,
    paddingBottom: 25,
  },

  formHeader: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 20,
  },

  formItem: {
    width: '100%',
    marginBottom: 15,
  },

  formLink: {
    textAlign: 'center',
  },
});